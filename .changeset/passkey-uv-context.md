---
"@emdash-cms/auth": minor
---

Adds configurable passkey user verification and typed, versioned WebAuthn challenge context while preserving preferred verification by default. Authentication now rejects authenticators whose signature counter falls from a nonzero value to zero.
